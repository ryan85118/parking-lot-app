<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    x:Class="parking_lot_app.Views.MyView"
    Width="525"
    Height="350"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Background="LightGray">
        <StackPanel Orientation="Vertical">
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="48"
                Foreground="White"
                Text="MyView" />
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="30"
                Text="{Binding Message}" />
            <Button HorizontalAlignment="Center" Command="{Binding GoNextCommand}" Content="導航到下頁"/>
        </StackPanel>
        <Button Content="Open File" Command="{Binding OpenFile}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Margin="21,29,0,0" FontSize="9"/>
    </Grid>
</UserControl>
